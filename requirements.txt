# Apache Airflow (required for vanilla Airflow, included in Astronomer Runtime)
apache-airflow==3.0.0

# Database Providers
# Note: We use OdbcConnectionHelper instead of apache-airflow-providers-microsoft-mssql
# to support corporate environments where the MSSQL provider may not be available
apache-airflow-providers-postgres>=5.12.0

# Database Connectivity
pyodbc>=5.0.0
psycopg2-binary>=2.9.9
